<UserControl x:Class="ImageViewer.ImageControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageViewer"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="Snow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Image x:Name="DisplayImage" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Width="50" Content="&lt;" Name="Prev_BTN" HorizontalAlignment="Right" Margin="10" Click="Prev_BTN_Click" />
            <Button Width="50" Content="&gt;" Name="Next_BTN" HorizontalAlignment="Left" Margin="10" Grid.Column="1" Click="Next_BTN_Click"/>

        </Grid>

    </Grid>
</UserControl>
